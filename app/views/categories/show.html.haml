/ PRODUCTS AREA STARTS
%section.section-products-area
  .container
    .row
      .col-md-10.col-sm-12.col-xs-12.pull-right
        -if @category.parent_id.blank?
          =render "featured_category"
        -else
          =render "product_list"
      .col-md-2.col-sm-12.col-xs-12
        =render "left_menu"
      / .col-md-2 ends
    / .row ends
  / .container ends
/ PRODUCTS AREA ENDS
